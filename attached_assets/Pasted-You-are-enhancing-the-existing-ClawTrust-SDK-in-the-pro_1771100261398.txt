You are enhancing the existing ClawTrust SDK in the project — the trust oracle for OpenClaw AI agents on Base Sepolia (using ERC-8004 for on-chain identity/reputation). The current SDK has ClawTrustClient with checkTrust(wallet), GET /api/trust-check/:wallet endpoint (using Drizzle DB for fusedScore, hasActiveDisputes, etc.), types, and README.

Goal: Make the SDK more trustless, powerful, and ecosystem-dominant by adding on-chain verification, confidence scoring, dispute links, batch checks, caching, and better docs — turning ClawTrust into verifiable infrastructure for A2A hiring/gigs in the scam-prone OpenClaw/Moltbook meta (Feb 2026).

Tasks — extend/refactor existing code:

1. Install viem if not present (npm install viem) for Base Sepolia RPC interactions.

2. Update types.ts:
   - Add to TrustCheckResponse:
     confidence: number;  // 0-1, higher if on-chain matches DB, recent data, diverse sources
     onChainVerified?: boolean;  // true if ERC-8004 rep score queried and matches fusedScore within tolerance
   - Add to AgentTrustProfile/details:
     disputeSummaryUrl?: string;  // e.g. "/disputes?agent=0x..." or external link
     onChainRepScore?: number;    // raw ERC-8004 reputation if queried

3. Update backend endpoint (/api/trust-check/:wallet):
   - After fetching agent from DB:
     - Compute fusedScore as before (60% on-chain if available, 40% Moltbook).
     - Apply decay: if (daysInactive > 30) effectiveScore *= 0.8;
     - hireable = effectiveScore >= 40 && !hasActiveDisputes;
   - Add on-chain verification (using viem):
     - Import { createPublicClient, http } from 'viem';
     - const client = createPublicClient({ chain: baseSepolia, transport: http(process.env.BASE_SEPOLIA_RPC_URL || 'https://sepolia.base.org') });
     - Query ERC-8004 Reputation Registry (assume deployed address in env: ERC8004_REP_REGISTRY_ADDRESS).
     - Example call: const onChainRep = await client.readContract({ address: ERC8004_REP_REGISTRY_ADDRESS, abi: [...ERC8004 ABI snippet for getReputation(agentId or wallet)], args: [walletOrAgentId] });
     - Normalize/compare: if Math.abs(onChainRep - fusedScore) > 10, set onChainVerified=false, confidence *= 0.7;
     - Add to response: onChainVerified, onChainRepScore: onChainRep || null
   - Add disputeSummaryUrl: e.g. `https://your-clawtrust.com/disputes?wallet=${wallet}` if hasActiveDisputes.
   - Compute confidence: base 0.8, +0.1 if onChainVerified, -0.2 if stale (>15 days lastActive), etc.
   - Handle errors gracefully (fallback to DB-only, log, don't crash).

4. Update ClawTrustClient in index.ts:
   - Add optional params to checkTrust(wallet: string, options?: { verifyOnChain?: boolean; apiKey?: string } = {}):
     - If verifyOnChain, pass ?verifyOnChain=true in query params to endpoint.
     - Add apiKey to headers if provided (Authorization: Bearer ${apiKey}).
     - Add simple in-memory cache (Map<string, {result: TrustCheckResponse, expiry: number}>, TTL 5 min).
     - Add retry logic (3 attempts, exponential backoff 1s/2s/4s) on network errors.
   - New method: async checkTrustBatch(wallets: string[], options?): Promise<Record<string, TrustCheckResponse>> — loop over checkTrust or batch endpoint if added later.
   - Stub for real-time: subscribeToWallet(wallet: string, callback: (update: Partial<TrustCheckResponse>) => void) — comment // TODO: implement WS later (e.g. via socket.io or native WS).

5. Enhance README_SDK.md:
   - Add sections:
     - On-Chain Verification: Explain ERC-8004 integration, how confidence/onChainVerified work.
     - Confidence & Probabilistic Scoring: Details on calculation.
     - Batch Checks & Real-Time: Usage examples.
     - Security: API key support, caching, retries.
     - Integration Examples:
       - Gig applicant filter: if (!result.hireable || result.confidence < 0.6) skip;
       - Swarm coord: batch check validators before vote acceptance.
       - Payment guard: reject x402/USDC if !hireable or low confidence.
   - Update response shape to include new fields.

6. Bonus polish:
   - Add env vars: BASE_SEPOLIA_RPC_URL, ERC8004_REP_REGISTRY_ADDRESS (fallback to mock if not set).
   - Rate limit endpoint more strictly for batch (e.g. 50/min).
   - Test helpers: Add mock on-chain responses for dev.

Priorities: Trustless on-chain layer first (viem query), then confidence/dispute links, then batch/caching. Keep clean/professional: robust errors, separation (viem in backend only), no glow/neon. Use mocks for ERC-8004 if registry not deployed yet (hardcode sample rep scores).

Generate/update files (types.ts, index.ts, endpoint route, README), show code diffs/full snippets for key changes (viem integration, client options, response shape), then suggest tests: curl with ?verifyOnChain=true, client.checkTrust with options.