You are building the main landing page / home page for ClawTrust — the reputation engine and gig marketplace for OpenClaw AI agents. Currently, the app opens directly to profiles or dashboards (e.g. /profile/:id). Now make the root path "/" the public landing page that greets everyone first.

Goal: Create a stunning, high-conversion home page that explains ClawTrust, builds excitement about the agent passport system (dynamic NFTs with fused reputation), and drives actions like connecting wallet, claiming passport, browsing gigs.

Key requirements:
- Route: Use wouter to set "/" as the landing page component (e.g. pages/Home.tsx or app/Home.tsx).
- Design: Clean/professional crypto aesthetic. Dark navy/black backgrounds (#020203 / #0a0a0f), strong OpenClaw orange-red accents (#FF4500 primary, #ff4d4d secondary), gold/silver rank tints. Fonts: Clash Display headings, Satoshi body, JetBrains Mono data. Subtle lobster/claw icons (LobsterIcon, ClawIcon). No heavy neon/glow/glassmorphism — elegant, futuristic, premium.
- Structure (full-page scroll):
  1. Hero section (full viewport height):
     - Background: subtle radial gradient + faint blockchain/circuit pattern.
     - Centered headline: "ClawTrust: The Passport & Trust Layer for OpenClaw Agents" (Clash Display, huge, orange-red).
     - Subheadline: "Give your AI agent a verifiable passport (dynamic NFT). Build fused reputation (on-chain ERC-8004 + Moltbook karma). Hire autonomously, complete gigs securely, and thrive in the agent economy."
     - Animated element: Evolving lobster/claw emblem with score ring (use framer-motion or CSS animation).
     - CTAs: Big orange "Connect Wallet & Claim Passport" button (Privy/Base Sepolia), secondary "Browse Gigs" / "Explore Passports".
  2. Features section (grid/cards):
     - 4-6 cards: "Dynamic Passports" (NFT-like evolving visuals), "Fused Reputation" (score breakdown), "Autonomous Gigs & Escrow", "Trust Oracle SDK", "Swarm Validation", "Moltbook Integration".
     - Each card: Icon (claw-themed), title, short description, subtle orange hover.
  3. How It Works (steps timeline):
     - Step 1: Connect wallet & mint/claim passport.
     - Step 2: Build rep via gigs/validation.
     - Step 3: Agents hire each other safely (SDK checks).
     - Step 4: Share passport on Moltbook/X.
  4. Stats / Trust signals:
     - Live counters (use TanStack Query): "X Agents Registered", "Y Gigs Completed", "Z Total Fused Score".
     - Or placeholders if no real data yet.
  5. Showcase (carousel or grid):
     - Preview dynamic passports (embed <PassportCard wallet="mock" /> or static images).
     - "Agents are leveling up" — show example ranks/skills.
  6. Footer:
     - Links: About, Docs, SDK GitHub, Moltbook, X, Privacy/Terms.
     - Copyright + subtle lobster mascot.
     - Social icons.

Technical:
- Use Shadcn components: Card, Button, Badge for ranks.
- Hero image/dynamic: Reuse/extend /api/passports/:wallet/image if possible, or static fallback hero.
- Responsiveness: Mobile-first, stack sections nicely.
- Animations: Subtle fade-in/scale on scroll (framer-motion optional).
- Wallet connect: Integrate Privy or wagmi for Base Sepolia (show connected state in hero).

Priorities: Make it visually premium & exciting (NFT collectible feel with orange dominance). Fast load, clear value prop. Test on mobile.

Generate/update:
- New Home.tsx (or landing component) with full structure.
- Any new Tailwind classes / components needed.
- Update router to make "/" the home page.
- Show key code: Hero JSX, features grid, CTAs.

After this, the app should open to this landing page first, not profiles/dashboards.